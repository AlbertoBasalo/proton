// ToDo : spy normalizePort has been called

// ToDo : spy app.listen has been called with a port
describe('have tests', () => {
  let sut;
  beforeEach(() => {
    // Arrange
    sut = null;
  });
  it('should ', () => {
    // Act
    const actual = null;
    // Assert
    const expected = null;
    expect(actual).toBe(expected);
  });
});
